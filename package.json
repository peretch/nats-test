{
  "name": "nats-test",
  "version": "1.0.0",
  "description": "This repository contains an example connecting node applications with a NATS Streaming Server by using node-nats-streaming npm package",
  "main": "index.js",
  "scripts": {
    "publish": "ts-node-dev --notify false src/publisher.ts",
    "listen": "ts-node-dev --notify false src/listener.ts"
  },
  "keywords": [
    "nats"
  ],
  "author": "sebastian@peretch.com",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^18.0.5",
    "node-nats-streaming": "^0.3.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  }
}
